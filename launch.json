{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "python-terminal",
            "request": "launch",
            "file": "${workspaceFolder}/src/main.py"
        }
    ]
}
